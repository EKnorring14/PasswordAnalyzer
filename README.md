Takes a password as user input to assess its strength  
Checks if the password meets a minimum length requirement (e.g., at least 8 characters)  
Verifies if the password includes at least one uppercase letter  
Ensures that the password contains at least one lowercase letter  
Checks if the password contains at least one digit  
Verifies the presence of at least one special character (e.g., !@#$%^&*()-+=)  
